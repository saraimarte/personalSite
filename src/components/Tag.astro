---
const {tags} = Astro.props;

 
interface Props{
    tags:String[];
}
---

<div class = "tags">
    {tags.map(tag => (
        <p class = "tag">{tag} </p>
    ))}
</div>
  

<style>
  
    .tags{
        display: flex;
        gap:0.5em;
    }
    
    .tag{
        text-decoration:none;
        background-color: blue;
        color:white;
        font-weight:bold;
        border-radius:20px;
        width:min-content;
        text-align:center;
        font-size:0.6rem;
        padding:5px 12px;
        margin: 0 0 10px 0;
        letter-spacing: 2px;
    }

</style>