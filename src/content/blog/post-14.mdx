---
layout: '@layouts/Blog.astro'
title: "Dev Log"
pubDate: 2025-01-08T00:00:00Z
author: 'Sarai Marte'
tags: ['webdev']
phase: 'rabbitHole'
draft: false 
slug: dev-log
---

import CB from '@components/CheckBox.astro';
import Card from '@components/Card.astro';

import img1 from '../../../public/img23.png';
import img2 from '../../../public/img24.png';

<CB c = "f"> Update the code for the code snippet component to reduce the marginal space on the right.  </CB>

<Card img = {img1}/>

<CB c = "f"> Update the code for content collections.</CB>

<CB c = "f"> Rewrite the code used to filter posts by phase.</CB>

<Card img = {img2}/>

<CB c = "f"> Fix the code component so that backticks don't break it. Using \ does not work. Using ```lang instead of the code component is a solution but not the one i'm looking for. </CB>

<CB c = "f"> Fix the re-organizing of content on load of a post. The problem is the title container's height. The content is adjusting to the length of the title.</CB>

<CB c = "t"> Fix the code component so that it doesn't have excess space at the bottom. Update: It was literally a css line I had to change but didn't see in the original theme file lol. </CB>

